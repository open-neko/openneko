--- a/acp_adapter/server.py
+++ b/acp_adapter/server.py
@@ -1943,6 +1943,29 @@ class HermesACPAgent(acp.Agent):
         usage_start = {key: getattr(state.agent, f"session_{key}_tokens", 0) for key in usage_fields}
         usage_calls_start = getattr(state.agent, "session_api_calls", 0)
 
+        def agent_number(name: str) -> float:
+            value = getattr(state.agent, name, 0)
+            return value if isinstance(value, (int, float)) and not isinstance(value, bool) else 0
+
+        cost_start = agent_number("session_estimated_cost_usd")
+        unknown_cost_calls_start = agent_number("session_cost_unknown_calls")
+        api_calls_start = agent_number("session_api_calls")
+
+        def cost_usage() -> dict[str, Any]:
+            agent = state.agent
+            api_calls = agent_number("session_api_calls") - api_calls_start
+            unknown_calls = agent_number("session_cost_unknown_calls") - unknown_cost_calls_start
+            return {
+                "api_calls": api_calls,
+                "cost_usd": max(0.0, float(agent_number("session_estimated_cost_usd")) - cost_start),
+                "cost_status": "unknown" if unknown_calls > 0 else getattr(agent, "session_cost_status", "unknown"),
+                "cost_source": getattr(agent, "session_cost_source", "none"),
+                "pricing_version": getattr(agent, "session_pricing_version", None),
+                "unknown_cost_calls": unknown_calls,
+                "provider": getattr(agent, "provider", None),
+                "model": getattr(agent, "model", None),
+            }
+
         if conn:
             tool_progress_cb = make_tool_progress_cb(
                 conn,
@@ -1952,8 +1975,11 @@ class HermesACPAgent(acp.Agent):
                 tool_call_meta,
                 edit_approval_policy_getter=lambda: self._edit_approval_policy_for_state(state),
                 usage_getter=lambda: {
-                    f"{key}_tokens": max(0, getattr(state.agent, f"session_{key}_tokens", 0) - usage_start[key])
-                    for key in usage_fields
+                    **{
+                        f"{key}_tokens": max(0, getattr(state.agent, f"session_{key}_tokens", 0) - usage_start[key])
+                        for key in usage_fields
+                    },
+                    **cost_usage(),
                 } if getattr(state.agent, "session_api_calls", 0) > usage_calls_start else None,
             )
             reasoning_cb = make_thinking_cb(conn, session_id, loop)
@@ -2236,7 +2262,10 @@ class HermesACPAgent(acp.Agent):
         await self._send_usage_update(state)
 
         stop_reason = "cancelled" if cancelled else "end_turn"
-        return PromptResponse(stop_reason=stop_reason, usage=usage)
+        field_meta = None
+        if agent_number("session_api_calls") > api_calls_start:
+            field_meta = {"openneko": {"usage": cost_usage()}}
+        return PromptResponse(stop_reason=stop_reason, usage=usage, field_meta=field_meta)
 
     # ---- Slash commands (headless) -------------------------------------------
 
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -3013,6 +3013,8 @@ def init_agent(
     agent.session_estimated_cost_usd = 0.0
     agent.session_cost_status = "unknown"
     agent.session_cost_source = "none"
+    agent.session_cost_unknown_calls = 0
+    agent.session_pricing_version = None
     # Rolling history for status-bar avg latency / velocity (last 10 calls).
     # Stored on the agent so both conversation_loop and codex_runtime share it
     # and the CLI snapshot can read it without extra IPC.
--- a/agent/codex_runtime.py
+++ b/agent/codex_runtime.py
@@ -209,8 +209,11 @@ def _record_codex_app_server_usage(agent, turn) -> dict[str, Any]:
     )
     if cost_result.amount_usd is not None:
         agent.session_estimated_cost_usd += float(cost_result.amount_usd)
+    else:
+        agent.session_cost_unknown_calls = getattr(agent, "session_cost_unknown_calls", 0) + 1
     agent.session_cost_status = cost_result.status
     agent.session_cost_source = cost_result.source
+    agent.session_pricing_version = cost_result.pricing_version
 
     if agent._session_db and agent.session_id:
         try:
--- a/agent/conversation_loop.py
+++ b/agent/conversation_loop.py
@@ -4442,6 +4442,8 @@ def run_conversation(
                     )
                     if cost_result.amount_usd is not None:
                         agent.session_estimated_cost_usd += float(cost_result.amount_usd)
+                    else:
+                        agent.session_cost_unknown_calls = getattr(agent, "session_cost_unknown_calls", 0) + 1
                     # Add MoA advisor cost (already priced per-advisor at each
                     # advisor's own model rate) on top of the aggregator cost.
                     if _moa_ref_cost is not None:
@@ -4451,6 +4453,7 @@ def run_conversation(
                             pass
                     agent.session_cost_status = cost_result.status
                     agent.session_cost_source = cost_result.source
+                    agent.session_pricing_version = cost_result.pricing_version
 
                     # Persist token counts to session DB for /insights.
                     # Do this for every platform with a session_id so non-CLI
--- a/agent/usage_pricing.py
+++ b/agent/usage_pricing.py
@@ -1035,6 +1035,42 @@ for _alias, _canonical in {
     ]
 del _alias, _canonical
 
+# Official prices that change on a known date. Each model maps to entries in
+# ascending ``effective_from`` order; the latest entry in effect at call time
+# applies. Source: https://ai.google.dev/gemini-api/docs/pricing (read 2026-09-17).
+_GEMINI_FLASH_2026 = PricingEntry(
+    input_cost_per_million=Decimal("0.75"),
+    output_cost_per_million=Decimal("3.75"),
+    cache_read_cost_per_million=Decimal("0.075"),
+    source="official_docs_snapshot",
+    source_url="https://ai.google.dev/gemini-api/docs/pricing",
+    pricing_version="google-pricing-2026-09-17",
+)
+_GEMINI_FLASH_2027 = PricingEntry(
+    input_cost_per_million=Decimal("1.50"),
+    output_cost_per_million=Decimal("7.50"),
+    cache_read_cost_per_million=Decimal("0.15"),
+    source="official_docs_snapshot",
+    source_url="https://ai.google.dev/gemini-api/docs/pricing",
+    pricing_version="google-pricing-2027-01-01",
+)
+_DATED_OFFICIAL_DOCS_PRICING: Dict[tuple[str, str], tuple[tuple[datetime, PricingEntry], ...]] = {
+    ("google", model): (
+        (datetime(1970, 1, 1, tzinfo=timezone.utc), _GEMINI_FLASH_2026),
+        (datetime(2027, 1, 1, tzinfo=timezone.utc), _GEMINI_FLASH_2027),
+    )
+    for model in ("gemini-3.7-flash", "gemini-3.8-flash")
+}
+
+
+def _dated_official_docs_pricing(key: tuple[str, str]) -> Optional[PricingEntry]:
+    now = _UTC_NOW()
+    current = None
+    for effective_from, entry in _DATED_OFFICIAL_DOCS_PRICING.get(key, ()):
+        if effective_from <= now:
+            current = entry
+    return current
+
 
 def _to_decimal(value: Any) -> Optional[Decimal]:
     if value is None:
@@ -1185,6 +1221,9 @@ def _normalize_anthropic_model_name(model: str) -> str:
 
 def _lookup_official_docs_pricing(route: BillingRoute) -> Optional[PricingEntry]:
     model = route.model.lower()
+    entry = _dated_official_docs_pricing((route.provider, model))
+    if entry:
+        return entry
     # Direct lookup first
     entry = _OFFICIAL_DOCS_PRICING.get((route.provider, model))
     if entry:
--- a/run_agent.py
+++ b/run_agent.py
@@ -805,6 +805,8 @@ class AIAgent:
         self.session_estimated_cost_usd = 0.0
         self.session_cost_status = "unknown"
         self.session_cost_source = "none"
+        self.session_cost_unknown_calls = 0
+        self.session_pricing_version = None
 
         # Session boundary: the usage anchor describes the OLD session's
         # transcript — a fresh/branched/resumed session must fall back to
