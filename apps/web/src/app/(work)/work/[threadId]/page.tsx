// WorkScreen is rendered by ../layout.tsx and reads the threadId from
// useParams(). The page exists only to register the /work/[threadId] route.
export default function WorkThreadPage() {
  return null;
}
