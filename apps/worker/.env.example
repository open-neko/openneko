# Worker uses no env vars. All config lives in:
#   ~/.neko/config.json   — DB connection (auto-populated by /setup)
#   ~/.neko/secret-key    — at-rest encryption key (auto-generated)
# plus the metadata DB itself for everything else.
